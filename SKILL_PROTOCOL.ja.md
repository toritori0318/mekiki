# SkillProtocol v1.0 — mekiki が検証する規約

スキルを書く人・レビューする人のための規約。機械検証の厳密仕様は
[docs/DESIGN.ja.md](docs/DESIGN.ja.md)、採用・不採用の判断は
[docs/DECISIONS.ja.md](docs/DECISIONS.ja.md)。本書は日々参照する要点のみ。

English: **[SKILL_PROTOCOL.md](SKILL_PROTOCOL.md)**

本文中の例と実測値は、mekiki が最初に適用されたコーパス（6プラグイン・約190スキル）に
由来する。スキル名は、自分の資産の中で見つけるべき「型」として読むこと。

規約の多くは `mekiki lint` が機械検証するが、**強制レベルは規則ごとに異なる**
（誤検知があり得るヒューリスティック規則は warn 固定、既存スキルは baseline で warn 降格、
散文でしか書けない規則は未強制）。対応表は §強制マップ。exit code は error のみ非0のため、
**warn 止まりの MUST 違反は CI ゲートを通過する** — そこはレビューで補う。

---

## 第一原理

> **決定的な処理はコードに、判断はプロンプトに、強制はフックに置く。仕様は宣言し、検証はコードで行う。**

SKILL.md はコンパイルされない。確率的なモデルが読んで解釈するだけ。守らせたい規則は
(a) 散文で「意図」を伝えるか (b) `scripts/`・`hooks/`・frontmatter で「機械的に強制」するかの二択であり、
曖昧に散文へ書いて守られることを期待してはならない（適用元の実測: 散文ガードスキルの起動が14日間で1回だった）。

## レイヤモデル

| 層 | 実体 | 責務 |
|---|---|---|
| L0 | frontmatter `name` / `description` | 発火判定（精度の半分はここで決まる） |
| L1 | SKILL.md 本文 | ワークフローと判断。先頭に Contract |
| L2 | `references/` | 必要時ロードの知識 |
| L3 | `assets/` | 機械コピー可能な出力テンプレ |
| L4 | `scripts/` | 決定的処理＋test（精度のもう半分） |

強制層（hook）の置き場は2つ: **plugin ルートの `hooks/hooks.json`**（セッション全体で常時有効）と、
**スキル frontmatter の `hooks:` キー**（そのスキルの実行中だけ有効で、終了時に自動解除。
全イベント対応）。スキル固有のガードは後者に置くとスキルと一緒に配布・移動できる。
lint は hook の中身を検査しない — §安全。

## 決定性境界 [MUST]

計算・閾値判定・SQL生成・データ整形・突合・外部書き込みペイロード変換・サニタイズ・スキーマ検証は
**`scripts/` に置き、test を同梱する**。SKILL.md 本文に決定的アルゴリズムを散文で書かない。
判断・統合・文章生成・仮説だけを散文に残す。
検出は **L6** — ヒューリスティックのため **warn 固定**（error にならない）。
CI を通っても違反があり得るのでレビューで見る。誤検知は理由付き suppress（§lint の抑制）。

### scripts のインターフェース [MUST]

エージェントは stdout/stderr を読んで次の行動を決める。人間向け CLI とは要件が違う。

- **対話プロンプトを出さない** — エージェントは非対話シェルで動くため TTY 入力待ちで**無限にハングする**。
  入力はフラグ・環境変数・stdin で受ける。必須引数の欠落は選択肢を列挙したエラーで即座に落とす
- **`--help` が主要インターフェース** — エージェントはこれで使い方を学ぶ。用途・フラグ・実行例を簡潔に
  （長い help もそのまま文脈を食う）。exit code の意味もここに書く
- **エラーは「何が起きたか・期待値・次の手」** — `Error: --format must be one of: json, csv, table.
  Received: "xml"` のように書く。`Error: invalid input` は1ターン無駄にする
- **stdout=構造化データ / stderr=診断** — JSON・CSV・TSV を使い、進捗や警告は stderr へ。
  空白整列のテーブルはパースさせない
- **冪等**（リトライされる前提。「あれば作らない」）／破壊的操作は `--dry-run` と明示フラグ
- **出力サイズを抑える** — ハーネスは10〜30K文字で出力を切る。大きくなるなら既定を要約にして
  `--offset` で追加取得させる、または `--output` を必須にする
- **依存は自己完結**（Python は PEP 723 のインライン宣言＋`uv run scripts/x.py`）。バージョンを固定する
- **意図的な簡略化にはマーカーを残す** — 既知のトレードオフ（粗いロック・O(n²)・素朴な
  ヒューリスティック等）は `# simplified: <性能/精度の天井>。昇格条件: <いつ本実装に上げるか>` の
  コメントで残す。suppress が「lint への抗弁」なのに対し、これは**自分の設計判断の記録** —
  無言の負債にせず `grep simplified:` で機械的に収穫できる形にする

## スキルを作る前に [MUST]

**新設は最後の手段**。カタログ常時コスト（§コンテキスト最小性: 平均243トークン/スキルが
発火の有無に関係なく毎セッション載る）はスキル数に比例するため、「存在すべきか」の判定が
内容の最小化より先に来る。次のラダーを上から順に検討し、上の段で足りるなら作らない:

1. **既存スキルの追記で足りるか** — モデルの誤りの訂正なら既存スキルの Gotchas に1行、
   発火の問題なら description を直す（新設せず、既存を育てる）
2. **CLAUDE.md の1〜2行で足りるか** — 手順ではなく「事実・規約」なら CLAUDE.md へ。
   逆に CLAUDE.md の一節が手順に育っていたらスキルへ切り出す（公式の使い分け基準）
3. **設定で足りるか** — 発火制御なら `paths`、強制なら `hooks`、権限なら `allowed-tools`。
   散文の指示より確実で、コンテキストを消費しない
4. **既存スキルの `references/` 追加で足りるか** — 知識の追加は新スキルでなく必要時ロード側へ
5. ここまで全部 No のときだけ新設する（`mekiki new` で生成。生成時にこのラダーを再確認させる）

新設したら §eval の with/without 比較で「無いよりマシ」を証明する。近傍スキルと発火が
競合するなら、双方の description / Non-goals に相互誘導を必ず書く（§L0）。
機械判定できないため**未強制** — 新規スキルのレビューの最初の問いにする。

## コンテキスト最小性 [MUST]

スキルの内容は「機能を実現するために必要な最小の高シグナル情報」だけにする。
SKILL.md はロードされた瞬間からシステムプロンプト・会話履歴と同じコンテキスト窓を消費し、
無関係な情報は「無害な余白」ではなく**精度を直接下げる**（distractor 1件の混入で性能低下、
本来解ける問題も落とすことが実測されている。根拠と出典は docs/DECISIONS.ja.md）。

本文の各段落は次の検問を通ったものだけ残す（書くとき・レビューするときの判定ロジック）:

1. **既知テスト**: モデルが既に知っている一般知識・概念解説・ツールの一般的な使い方を
   書かない（"Claude is already smart"。例:「PDFとは」「勝率の意味」「pandas の基本」）
2. **削除テスト**: その段落を消してスキルの挙動が変わらないなら消す
   （"Does this paragraph justify its token cost?"）
3. **行動テスト**: 「何をするか・どう判断するか」だけを書く。背景説明・経緯・開発メモの
   物語を書かない（Why はコミットログ・決定記録（docs/DECISIONS.ja.md）側へ — 本書冒頭の情報配置原則）。
   エッジケースの網羅列挙ではなく代表例を厳選する
4. **層テスト**: 毎回必要 → 本文 / 特定分岐でのみ必要 → `references/`（必要時ロード）/
   決定的 → `scripts/`（実行されるだけで内容はロードされない）。本文は目次＋ワークフローに
   徹する。references/ が大きいこと自体は無害（読まれるまで消費ゼロ）
5. **重複・矛盾テスト**: 発火条件は description にだけ書く（本文に再掲しない）。同じ事実を
   2箇所に書かない。時限情報（「現在は」「新方式では」）・矛盾する新旧記述を残さない

サイズ規律: 公式目安は **500行未満かつ 5,000 トークン未満**の2本立て
（"under 500 lines and 5,000 tokens"）。**日本語では行数だけの規律は空振りする** —
実測で本文は1行平均75.8字あり、167スキル中 500行超は1件だけなのに本文5,000字以上は93件だった。
**L19** は行数と推定トークン（CJK 1字=1.2トークン概算 — 新世代トークナイザで CJK が15〜35%増との計測報告に合わせた係数）の両方を見る。
サイズは適切性の**代理指標**にすぎないため **warn 固定**（CI は通過する）。
検問 1〜5 は機械判定できない（**未強制**）— 新規スキルのレビューで見る。

**カタログの常時コスト**: `name`＋`description`（＋`when_to_use`）は**発火の有無に関係なく
毎セッション全スキル分がロードされる**（progressive disclosure の Tier 1。公式の想定は
1スキル 50〜100 トークン）。本文サイズと違い、これは**無条件に払うコスト**でスキル数に比例して増える。
実測: 174スキルで **約42,250 トークン**（平均243／スキル。公式想定の2〜3倍。
when_to_use 込み・CJK 1字=1.2トークン換算）。
description を膨らませるほど全セッションが重くなるため、発火精度に効く語だけを残す
（発火精度と冗長さのトレードオフは §eval の発火精度測定で個別に判断する）。
人間しか起動しないスキルに `disable-model-invocation: true` を付けると**カタログから完全に消える**
ため、この常時コストも払わなくなる（§安全）。

なお検問3の「Why を書かない」は**プロジェクト史・開発経緯**を指す。
**指示そのものの理由**（「Y が起きやすいので X する」）は書く — 公式は
"explaining *why* can be more effective than rigid directives" とし、目的を理解した
モデルの方が文脈依存の判断を誤らないとする。ALL-CAPS の硬直命令より理由付きの方が従われる。

## ディレクトリ [MUST]

- 参照は `references/`（複数形）、コードは `scripts/`、テンプレは `assets/` に固定。`reference/`・`bin/`・`assets/scripts/` は不可。
- ビルド成果物（`*.zip` 等）をリポジトリに置かない。

## frontmatter

**公式キー**（Agent Skills 標準＋Claude Code 拡張。ランタイムが解釈する。2026-07-29 時点）:

```yaml
name: <必須。最大64字・小文字英数とハイフンのみ・先頭末尾と連続ハイフン不可・ディレクトリ名と同一>
description: <必須。最大1024字（本書の運用は150〜400字）。§L0 参照>
when_to_use: <発火条件の補足。description に連結され、合算1,536字までスキル一覧に載る>
paths: ['src/**/*.ts']  # 一致するファイルを扱うときだけ自動発火（過剰発火の抑制。§L0）
allowed-tools: <確認なしで使えるツール。付与であって制限ではない（§安全）>
disallowed-tools: <実行中プールから外すツール>
disable-model-invocation: true  # 副作用のあるスキルは必須（§安全）。Claude の自律起動を止める
user-invocable: false   # 内部スキル（他スキルからのみ起動）は必須。/ メニューから隠すだけ
context: fork           # サブエージェントで実行（文脈を隔離したい場合）
agent: <context: fork 時のサブエージェント種別>
background: false       # fork の結果を同一ターンで待つ（既定はバックグラウンド）
model: <このスキル実行中だけのモデル上書き>   # effort: と併せ、決定性が要る処理の固定に使える
effort: low             # このスキル実行中だけの effort 上書き
hooks: {...}            # スキル実行中のみ有効な hook（§レイヤモデル・§安全）
arguments: [issue]      # 位置引数の命名。本文で $issue / $ARGUMENTS が展開される
argument-hint: '[issue-number]'  # / 補完時のヒント
shell: bash             # 本文のインラインコマンドを実行するシェル
license: <ライセンス名 or 同梱ファイル名>
compatibility: <最大500字。必要な環境・パッケージ・ネットワーク等。大半のスキルは不要>
metadata: {author: <org>, version: "1.0"}  # 任意の文字列キー・値
```

**プロトコル独自キー**（ランタイムは解釈しない。lint・viz・監査だけが読む）:

```yaml
status: active          # active | deprecated | experimental。非推奨は description でなくここに書く
canonical: <plugin:skill> # 重複・共有元がある場合、正本を指す
duplicate_of: <plugin:skill> # 意図的な複製側に置き、正本を指す（§単一ソース）
requires: [<ガードスキル>] # リスク階層に応じて必須。宣言だけではガードは起動しない（§安全）
depends_on: [<artifact>]  # 前段スキルの成果物に依存する場合
```

- [MUST] 発火条件の正は description（`when_to_use` は公式キーで Claude Code では発火に効くが、
  **他クライアントは読まない**ため可搬性が要るスキルでは description に書く）。
  合算が1,536字を超えると一覧で切り詰められ、超過分は発火判定に載らない（lint L2）
- [MUST] description に `【非推奨】` 等のライフサイクル語・開発メモを混ぜない
- 独自キーは**宣言したから効く、は誤り**。実際の挙動は本文の指示・`scripts/`・hook で実現する
- `user-invocable: false` は **/ メニューから隠すだけ**で Skill ツール経由の起動は防げない。
  Claude 自身に起動させたくない場合は `disable-model-invocation: true` を使う（別物）
- `allowed-tools` は**権限の付与**であり制限ではない。スキルは自分に広いツール権限を与えられるため、
  外部由来のスキルは信頼する前に中身を読む（公式が明示的に警告している）

## L0: 命名と description

**命名 [MUST]** — 公式制約。違反は **L1**:
1〜64字／小文字英数とハイフンのみ／先頭・末尾のハイフン不可／連続ハイフン (`--`) 不可／
**ディレクトリ名と完全一致**。予約語 `anthropic` / `claude` を含めない。

**命名の型 [SHOULD]** — 機械検証しない（下表の理由）:

| 種別 | 推奨 | 例 |
|---|---|---|
| アクション | `動詞ing-対象` | `drafting-weekly-plan` |
| ナレッジ提供 | `knowledge-<領域>` | `knowledge-shared-master` |
| 共有ユーティリティ | 名詞・3語以内 | `billing-guard` |

かつてこの3分類を機械強制していたが、**実測で25件を指摘したうちすべてが公式的に有効な名前**
（`google-ads-account-setup` 等）で、さらに104件が「名詞3語以内」の catch-all を通っており
動名詞要件は実質機能していなかった。改名は既存の参照を壊すため行動可能でもない。
**名前は発火の主要因ではない**（発火は description が担う）ため、ここに強制コストを払わない。

**description [MUST]** — 150〜400字で次の3要素をこの順に:
1. 何をするか（三人称）
2. いつ発火するか（症状語・依頼語を具体列挙）
3. いつ発火しないか（近傍スキルへ誘導）

手本: `internal/lint` のテストにある fixture `querying-warehouse` の「症状ベース＋近傍誘導」。

**paths [SHOULD]** — 特定のファイル種別・ディレクトリを扱うときだけ意味があるスキル
（特定言語のコーディング規約、特定ディレクトリの運用手順等）は `paths:` の glob で自動発火を
制限する。description の文言だけで発火を絞るより確実で、無関係なタスクでの過剰発火
（＝カタログ以外のコンテキスト消費）を機械的に防げる。

## L1: Contract [MUST]

SKILL.md の最初の `##` 見出しは `## Contract`:

```markdown
## Contract
- **Trigger**: 発火状況を1文（description と整合）
- **Inputs**: required / optional（各々どう取得するか）
- **Preconditions**: 実行前に真であるべき条件（可能なら検証コマンド）
- **Outputs**: 生成ファイルのパスと形式
- **Postconditions**: 完了判定と検証方法（可能なら scripts のゲート）
- **Non-goals**: やらないこと。近傍スキル・前後工程への委譲
```

Non-goals が最重要（スコープ越境・前工程再実装の防止）。
Pre/Postconditions は文章でなく**実行可能なゲート**を推奨（適用元の手本: 検証 script が exit 0 になるまで完了扱いにしない）。

## L1: 本文の構成 [SHOULD]

Contract の後に置く定型パターン。すべて要るわけではなく、タスクに合うものを選ぶ。

### Gotchas（環境固有で直感に反する事実）

公式が **"the highest-value content in many skills"** と呼ぶ枠。一般論
（「エラーを適切に処理する」）ではなく、**知らなければモデルが確実に間違えること**を書く:

```markdown
## Gotchas
- `users` テーブルは論理削除。`WHERE deleted_at IS NULL` が無いと退会済みが混じる
- 同じ値が DB では `user_id`、認証では `uid`、請求APIでは `accountId`
- `/health` は DB 断でも 200 を返す。実際の死活は `/ready`
```

- **`references/` に逃がさず本文に置く** — その状況に遭遇したことをモデルが自覚できないと
  参照ファイルを読むきっかけが無い
- **モデルを訂正したら、その都度ここに1行足す**（スキルを継続改善する最短経路）

### その他のパターン

| パターン | 使う場面 | 要点 |
|---|---|---|
| 出力テンプレ | 形式を守らせたい | 散文で説明せず**雛形を貼る**（モデルは構造に強い）。長い/条件付きなら `assets/` へ |
| チェックリスト | 依存・検証ゲートのある多段手順 | `- [ ]` でモデルが進捗を追える。手順飛ばしを防ぐ |
| 検証ループ | 出力品質を上げたい | 作業 → 検証コマンド → 直す → **通るまで繰り返す**と明記 |
| plan-validate-execute | 破壊的・一括操作 | 中間プランをファイルに書く → script で正本と突合 → 実行。検証エラーに**利用可能な選択肢**を含めるとモデルが自己修正できる |
| atomic deliver | 既存成果物の更新 | 一時パスに書く → 検証ゲート → 通ったら置換。**検証に通らない出力で既存物を上書きしない**（fail-closed。上書き自体は `--force` ガード経由 — §安全） |

指示の厳しさはタスクの脆さに合わせる（**自由度の調整**）。壊れやすい・順序が決まっている操作は
「このコマンドを正確に実行し、変更しない」と書く。複数の正解がある判断は方向とその理由だけ与える。
選択肢を並べず**既定を1つ**示し、代替は短く添える。

他スキルを順に呼び出すワークフロー統括スキルは、呼び出し順序と
フェーズ間の入出力を **機械可読な `flow.json`** で宣言する。散文のデータフロー図や
Phase Registry 表「だけ」に頼らない（人間は読めても機械が検証・可視化できない）。

`<orchestrator>/flow.json`:

```json
{
  "flow": [
    {"phase": 1, "skill": "collecting-context",
     "inputs": ["target_id"], "outputs": ["01_context.json"]},
    {"phase": 2, "skill": "drafting-strategy",
     "inputs": ["01_context.json"], "outputs": ["02_strategy.json"]}
  ]
}
```

- [MUST] `flow[]` の各要素は `phase`(int) / `skill`(str) / `inputs`(list) / `outputs`(list) を持つ。フェーズ番号は一意で昇順（**L15**: error）
- [MUST] `flow[].skill` は実在するスキル名（**L16**: error）
- [MUST] ある input が別フェーズの output と同名なら、先行フェーズで生成されていること（順序整合。**L17**: **warn** — CI ゲートは通過するのでレビューで見る）。外部入力（ユーザー指定の `store_slug` 等、どの output でもないもの）はそのまま列挙してよい
- **`flow.json` の存在自体は未強制**（「統括スキルかどうか」の機械判定が困難なため、L15〜L17 は
  ファイルが存在する場合のみ検査する）。統括スキルの新設・レビュー時に人が確認する
- この宣言により `mekiki atlas` がフロー図（順序・input/output）を**推定なしで**描画する。`flow.json` が無い場合は本文の Phase Registry 表からの**推定**表示に留まる

## 成果物 [MUST]

- ファイルとして書き出す（会話貼り付けで終えない）
- パス: `<base>/<store>/<skill>/{YYYYMMDD}_<slug>/NN_<artifact>.<ext>`。ディレクトリ名は ASCII のみ
- 数値の Single Source of Truth は1ファイルに固定し、後続は `_ref` 参照のみ（再計算・手転記禁止）
- [SHOULD] **構造を持つ成果物（計画・診断・スケジュール等）は IR を正とする** —
  モデルには md を直接書かせず、**スキーマ検証可能な JSON（中間表現）を書かせて script が
  検証・変換する**。人間可読の md はそこから生成するビュー（マスタパッケージの
  `tables/`→`views/`、`flow.json`→Atlas と同型）。判断の出力自体が機械検証の対象になり、
  体裁の揺れ・数値の手転記・目視でしか検証できない成果物を排除できる。
  検証に通らない IR から md を生成しない（fail-closed）

## データ形式の選択

| 使われ方 | 形式 |
|---|---|
| 読み物・自由記述 | Markdown |
| 構造化・差分検知 | YAML |
| 大量追記の履歴 | JSONL（append-only） |
| 実行用クエリ | `.sql` |
| 大量リレーションの結合・集計 | SQLite＋script（破綻してから移行。先回りしない） |

- [MUST] リレーションを markdown 表でモデルに結合させない。関係は ID で表現
- [SHOULD] 不変スナップショット（baseline）と可変ビュー（current）を分離

## 単一ソース [MUST]

共有知識・マスタ・共通ルール・共有スクリプトは1箇所のみ。物理コピー禁止。
例外は2つで、いずれも「正本と再生成手段が機械可読」であること:
1. `canonical` / `duplicate_of` の frontmatter 宣言
2. **生成バナー付きの焼き込みコピー** — ファイル先頭コメントに AUTO-GENERATED 等のマーカー＋再生成/同期コマンドを持つ配布コピー（例: `master/` → 各スキル `references/master/` のファンアウト）。CI の drift 検知とセットで運用する

宣言なき同一コピーは lint error、宣言/バナー付きは warn。

## マスタデータの書式 [MUST]（AI が読む前提の規約）

マスタ（カタログ・辞書・対応表）を Markdown で配布する場合:

1. **出所バナー**: 先頭コメントに「自動生成である旨・一次ソース・再生成コマンド」を必ず書く
2. **廃止情報はファイル自身が持つ**: 「このファイルは旧版・参照禁止」等の状態は README ではなく
   **当該ファイルの1行目バナー**に書く。README はファンアウトに同行しないため、コピー先で警告が消える
3. **閉世界宣言と件数**: 「全 N 件。ここに無いものは存在しない（生成禁止）」を冒頭に明記する
   （ハルシネーション抑制。列挙の網羅性を AI が自己検証できる）
4. **ID は ASCII slug で表示名と分離**: `community` は可、`omoモデル`・`コンバージョン率` のような
   表示名兼用 ID は不可（改名で参照が壊れる・照合が揺れる）。関係は ID で張る
5. **構造を平坦化しない**: チェックリスト・番号付き手順は改行と箇条書きを維持して出力する。
   1行に数千字を詰めない（目安: 1行500字以内）。行指向ツール（Read の部分読み・grep -n・diff）と
   見出しベースのチャンク分割が壊れるため
6. **一覧→詳細の2段構成＋読み方指示**: 冒頭にサマリ表と目次、「必要な `ID` セクションだけ読め」を明記
7. **リレーションは構造化データを正とする**: N×M の対応表は JSON/CSV を正本にし、
   Markdown は主要アクセスパターン方向の生成ビューとして配る（逆引き・集計・整合性検証は
   構造化データに対して script で行う）。◎○△×の密行列など「全体を一望する」用途のみ md 表を正としてよい
8. **空カラムを温存しない**: 全行空の列は落とすか「未整備」と明記する（データが在る誤解を防ぐ）

### 推奨レイアウト: マスタパッケージ [SHOULD]

リレーションを含むマスタは、次の3層パッケージで配布する
（実証 POC は社内マスタデータの実物を含むため配布物から除外した — 初期コミットの git 履歴
`poc/master-package/` に構成・再生成手順・実測の検出事例が残っている）:

```
master/
├── schema.json      # Table Schema 互換: テーブル・PK・FK 宣言（独自形式を発明しない）
├── tables/*.jsonl   # 構造化原本（正）。1行1レコード=git diff が行単位で効く。
│                    #   リレーションは ID のみ持ち、名前等は FK 先から引く（非正規化しない）
├── narratives/**.md # 長文フィールドの原本（正）。見出し・改行を維持し、jsonl からパス参照
└── views/*.md       # 読み手向け生成ビュー（一覧→詳細・目次・閉世界宣言つき）
```

- 読む AI は `views/`、検証・集計・逆引きは `tables/`＋script（アクセスパターンで正本を分ける）。
  **`tables/` を AI が丸読みしてはならない** — script 経由で答えだけを文脈に入れる
- `schema.json` に基づく PK/FK 検証を CI に置く（drift 検知の拡張）
- **サイズ規律**（判断基準はディスクではなく「タスクあたり文脈トークン」）:
  - 導出可能な列（FK 先から引ける名前、ID 接頭辞から分かるカテゴリ等）は正本に持たない。
    `schema.json` の `derived` に導出規則を宣言する
  - 大きいリレーションはオブジェクト行でなく**配列行**（schema で列名宣言）または
    **隣接リスト**（`{"model_id": x, "measures": [...]}`）を使ってよい
    — 実測: 866関連で md 53.6KB / オブジェクト行 90.7KB / 導出列削除 59.1KB /
    配列行 36.6KB（0.68x）/ 隣接リスト 15.9KB（**0.30x**）。md が最小という前提は誤り
- YAML はテーブルデータに使わない（アンカーは FK にならない・パーサ複雑・大量レコードに不向き。
  YAML の適所は少量の状態/設定ファイルのみ）。LTSV も不採用（改行/タブを含む値・ネスト・配列が
  表現できず、スキーマ生態系が無い。JSONL が全面的に優越）

## 安全: 二層防御 [MUST]

決定的に判定できる安全則は hook で強制。散文は一次防御まで。リスク階層と必須ガード
（ガードスキル名は組織ごとに異なるため、mekiki は `config.json` の `guards` から読む。
未設定なら検査しない — 名前をコードに埋めると他組織で存在しないスキルを要求してしまう）:

| リスク | 必須 |
|---|---|
| 課金発生 | `requires: [<billing ガード>]`＋`disable-model-invocation: true` |
| 破壊的書込 | `requires: [<write ガード>]` または write ガード script |
| ブラウザ自動操作 | `requires: [<browser ガード>]` |
| 外部公開 | Contract Preconditions に事前確認を明記＋`disable-model-invocation: true` |

**副作用のあるスキルは `disable-model-invocation: true` [MUST]** — これは
`requires` と違い**ランタイムが実際に解釈する**唯一の発火抑止機構で、Claude が自律的に
そのスキルを起動できなくなる（人間が `/<skill>` で明示起動したときだけ動く）。公式は
デプロイ・コミット・外部送信を例に挙げ「コードが良さそうに見えるからと Claude がデプロイを
決めてしまうのは避けたい」としている。課金・外部公開・不可逆な書き込みを伴うスキルは必ず付ける。
ツールを実行中に限って外したい場合は `disallowed-tools` を使う。

- 課金スキルの未設定は **L20**（warn）。外部公開・破壊的書込も規約上は必須だが、
  検出語が広く実測で167件中114件（68%）に当たり規則として行動可能でないため
  **機械検証しない**（規約上の要求としては残しレビューで見る）
- **`flow.json` の委譲先には付けない** — このフラグは programmatic invocation を止めるため、
  付けるとオーケストレータが `Skill` ツールで呼び出せなくなる。統括スキル側にだけ付ける
- カタログから完全に消えるため、常時コスト（§コンテキスト最小性）も払わなくなる

`requires` は**宣言だけではガードを起動しない**（ランタイムは解釈しない監査用メタデータ）。
本文に**命令形の起動指示**（「作業前に `Skill` ツールで billing-guard を起動する」）を必ず書く
（受動的な参照はバイパスされる — 上記の実測）。本文にガード名の言及が無い場合は **L18**（warn）。
hook の置き場は plugin ルートの `hooks/hooks.json`、またはスキル frontmatter の `hooks:`
（スキル実行中のみ有効・終了時に自動解除 — §レイヤモデル）。**スキルに同梱できるようになった**ため、
「guard script が別 plugin にあるので二層目が組めない」は理由にならない。

高リスク操作は hook＋スキル内 preflight の二重化（一部実行環境でプラグインレベル hook が
発火しない既知の制約への対策）。書き込みは冪等に、上書きは `--force` 必須のガード経由で。

## eval [MUST]

**公式（Agent Skills 標準）の `evals/evals.json` を使う。独自形式を発明しない**
（本書がマスタデータに課している規律を eval にも適用する）。スキーマはdocs/DESIGN.ja.md §4.8、
一次ソースは https://agentskills.io/skill-creation/evaluating-skills 。

```json
{"skill_name": "<skill>", "evals": [
  {"id": 1, "prompt": "<現実的なユーザー依頼文>", "expected_output": "<成功状態の記述>",
   "files": ["evals/files/input.csv"], "assertions": ["検証可能な言明"]}
]}
```

- **assertions は初回実行の後に書く**（何が「良い」かは走らせるまで分からない）。
  検証可能・観測可能・数えられる言明にする。文体や見栄えは assertion にせず人のレビューで見る
- **両構成で常に pass する assertion は削除する**（スキルの価値を示さないのに pass 率を膨らませる）

### 出力品質の測定 [MUST]（スキルが「無いよりマシ」の証明）

**同じケースを with_skill / without_skill の2回走らせ、差分で判断する**。
改修時は改修前スナップショットを baseline にする。

- `delta` として **pass 率・所要時間・トークン**の3つを取る。トークン増に見合う改善かを見る
  （§コンテキスト最小性を実測で裏付ける唯一の手段）
- **スキル無しでも十分こなせるなら、そのスキルは価値を足していない**（削除を検討する）
- 各 run は**クリーンな文脈**で開始する（サブエージェント or 別セッション）
- 同じ eval が run ごとに揺れる場合、eval が脆いか**指示が曖昧**。例示を足して曖昧さを減らす

### 発火精度の測定 [MUST]（L0 は精度の半分を決めるため）

description の発火は**統計的に**測る。1件のケースでは何も保証できない（モデルは非決定的で、
同じ依頼文でも発火が揺れる）。

- 約20クエリ（**発火すべき 8〜10 件＋発火すべきでない 8〜10 件**）。後者は
  **near-miss**（語や概念が重なるが別スキルに行くべきもの）にする。無関係なクエリは何も検証しない
- **各クエリを3回**実行し trigger_rate を出す（閾値 0.5 が既定）
- **train 60% / validation 40% に分割**する。train の失敗だけを改修根拠にし、
  validation は汎化の確認だけに使う。最良イテレーションは validation の pass 率で選ぶ
  （最後のイテレーションが最良とは限らない — 過学習しているため）
- 失敗クエリの語をそのまま description に足すのは過学習。**その語が属する一般カテゴリ**を書く

**L13** は形式のみ検査する（error）。**eval の不在・runner の実行は未強制**
（Tier 報告のみ。runner は `skill-creator` がこのワークフローを自動化するので自作しない）。

### 実行結果を記録する [SHOULD]

eval ファイルを持っていることは「ケースが存在する」証明であって「通っている」証明ではない。
実行して何が起きたかは `evals/results.json` に書く。**L25** がこれを読み、下の成熟度 Tier も参照する:

```json
{
  "ran_at": "2026-09-03",
  "evals": [{"id": "case-1", "pass": true}, {"id": "case-2", "pass": false}],
  "queries": {"total": 20, "correct": 18}
}
```

どちらのキーも任意なので、片方だけの実行を記録してもよい。**ファイルが無い場合は何も
言わない（指摘にならない）** —— これが既存資産での段階的採用を可能にしている。存在する
ファイルは正しい形式であること（error）、失敗ケースがあれば warn。発火精度は記録するが
合否は判定しない。合格ラインには閾値が要るが、それを正直に決められる実測がここには無い。

## 成熟度 Tier

本プロトコル独自の尺度（公式標準に成熟度モデルは無い）。設計はソフトウェア成果物の
保証レベル SLSA（Level 0〜3・累積条件。L0=未達の床）と同型で、各 Tier の条件は
**本規約の lint 規則に1対1で対応**する（散文の到達目標ではなく機械判定できる到達度）。
**公式文書の "Tier 1〜3"（progressive disclosure のロード段階: カタログ→本文→リソース）とは
無関係**なので混同しないこと。

| Tier | 条件（累積。判定は対応 lint 規則の指摘なし） | 適用 |
|---|---|---|
| 0 存在する | 要件なし（SLSA L0 と同じ「未達」の床） | — |
| 1 起動し、契約がある | L1/L2/L3 の error なし ＋ Contract 6項目（L4） | **全新規スキルの最低ライン** |
| 2 ＋決定的で安全 | L6・L8・L9・L18・L20 の指摘なし | **高リスクスキルは必須** |
| 3 ＋検証される | 公式 eval 2形式（`evals.json`＋`eval_queries.json`）が存在し、L13 error も L25 指摘も無い（記録された実行があるなら合格している） | 基幹・オーケストレーション配下 |

意味論（誤解されやすい点を固定する）:
- **該当なしは適合** — 決定的処理もリスク語も無い知識系スキルは T2 の条件を空虚に満たす（守るものが無い状態は「安全」）
- **理由付き suppress は有効** — suppress は理由の明記を強制しており、その判断ごと Tier に反映される
- **Tier は総合値、軸別の真値は viz のバッジ**（contract dots / scr / test / eval）。単一ラダーは
  「別軸の未達が進捗を隠す」性質を持つ（SLSA が v1.0 でトラック分離した理由）ため、軸別の進捗はバッジで見る
- **スコープ: 第一者（自作スキル）の工学品質の尺度**であり、第三者スキルの供給網トラスト
  （マルウェア・prompt injection・作者身元の審査）は対象外。外部由来スキルの受け入れ判断は
  §frontmatter の「信頼する前に中身を読む」と組織の審査プロセスが担う

## 強制マップ（規則 → 機械検証）

本書の第一原理（散文で守られることを期待しない）を本書自身に適用し、
どの規則が機械強制され、どれが散文（＝レビュー担保）かを明示する。
「新規/既存」は baseline.json 非掲載/掲載のスキル。**warn と未強制の規則は CI を通過する**。

| 規則 | lint | severity |
|---|---|---|
| 命名の公式制約（64字・文字種・ディレクトリ一致）[MUST] | L1 | 新規 error / 既存 warn |
| 命名の型（動名詞・knowledge-*）[SHOULD] | — | **未強制**（強制すると有効名を25件誤指摘し、104件は catch-all を通っていた） |
| 本文の構成（Gotchas 等）[SHOULD] | — | **未強制**（レビュー担保） |
| scripts のインターフェース [MUST] | — | **未強制**（レビュー担保） |
| description 150〜400字＋発火語（when_to_use と合算判定）＋一覧1,536字上限 [MUST] | L2 | 字数: 新規 error / 既存 warn。発火語・上限超過: warn |
| ライフサイクル語の禁止 [MUST] | L3 | error |
| Contract [MUST] | L4 | Non-goals 欠落: error。他: 新規 error / 既存 warn |
| ディレクトリ規約 [MUST] | L5 / L10 | 新規 error / 既存 warn。ビルド成果物: error |
| 決定性境界 [MUST] | L6 | **warn 固定**（ヒューリスティック） |
| コンテキスト最小性: 500行＋5,000トークン未満 [MUST] | L19 | **warn 固定**（サイズは代理指標） |
| コンテキスト最小性: 検問1〜5 [MUST] | — | **未強制**（レビュー担保） |
| スキルを作る前に: 新設ラダー [MUST] | — | **未強制**（レビューの最初の問い。scaffold が生成時に再確認を促す） |
| 成果物の IR 化 / atomic deliver / 簡略化マーカー [SHOULD] | — | **未強制**（レビュー担保） |
| scripts の test 同梱 [MUST] | L9 | 新規 error / 既存 warn |
| 単一ソース [MUST] | L7 / L14 | error（`canonical`/`duplicate_of`/生成バナー付きは warn） |
| 内部スキルの `user-invocable` [MUST] | L11 | error |
| 非標準 frontmatter キー | L12 | warn |
| リスクガードの `requires` 宣言 [MUST] | L8 | error（ガード名を `config.json` に設定した場合のみ。未設定なら検査しない） |
| ガードの本文起動指示 [MUST] | L18 | warn（言及の有無のみ機械判定） |
| 副作用スキルの `disable-model-invocation` [MUST] | L20 | **課金のみ warn**。外部公開・破壊的書込は**未強制**（検出語が68%に当たり行動可能でない） |
| flow.json のスキーマ・実在・順序 [MUST] | L15 / L16 / L17 | error / error / **warn** |
| 中間 artifact が必ず消費される [SHOULD] | L24 | warn（最終フェーズは対象外） |
| スキル間で発火句が競合しない [MUST] | L23 | 2スキルが同じ引用句を主張していれば warn |
| `requires` / `depends_on` の参照先が実在 [MUST] | L21 | warn（資産の一部だけの lint では他プラグイン参照を解決できないため） |
| `references/`・`scripts/`・`assets/`・`evals/` へのリンクが解決 [MUST] | L22 | warn。**本文中の単なるパス言及は未強制**（実測で例示であり参照ではなかった） |
| flow.json の存在自体 [MUST] | — | **未強制**（統括スキルの機械判定が困難） |
| eval が公式形式（`evals/evals.json`）[MUST] | L13 | 形式不適合: error。旧 `cases.json`・別名: warn。**不在は未強制**（Tier 報告のみ） |
| eval の with/without 比較・発火精度の統計測定 [MUST] | — | **未強制**（runner は `skill-creator` に委譲） |
| `evals/results.json` への実行結果記録 [SHOULD] | L25 | 形式不適合: error。失敗ケースあり: warn。**不在は未強制** |
| 成果物パス・数値 SSOT `_ref` [MUST] | — | **未強制**（レビュー担保） |
| マスタデータの書式 1〜8 [MUST] | — | **未強制**（PK/FK 検証はパッケージ側 script — 手本: POC `validate_package.py`） |

## 抑制（suppress）

誤検知は理由付きで抑制する（理由なしは無効）:

```markdown
<!-- mekiki: disable L6 -- 判断基準の説明であり実計算は scripts/calc.py が行う -->
```
